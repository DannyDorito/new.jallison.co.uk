import "@testing-library/jest-dom";
import { render, screen } from "@testing-library/react";
import { MockedProvider } from "@apollo/client/testing";

const mocks = [];

it("Renders GitHub bio without error", async () => {
  render(
    <MockedProvider>
    
    </MockedProvider>
  );
});
